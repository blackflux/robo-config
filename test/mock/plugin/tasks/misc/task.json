{

  "target": "${misc}.txt",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "snippet",
      "variables": {
        "variable": "${variable}"
      }
    }
  ],
  "requires": [
    "dependency"
  ],
  "purpose": [
    "Some purpose."
  ]
}
