{

  "target": "${misc}.txt",
  "strategy": "overwrite",
  "create": true,
  "snippets": [
    {
      "name": "snippet",
      "variables": {
        "variable": "${variable}"
      }
    }
  ],
  "requires": [
    "dependency"
  ],
  "purpose": [
    "Some purpose."
  ]
}
