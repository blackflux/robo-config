{
  "target": "${misc}.yml",
  "strategy": "overwrite",
  "snippets": [
    {
      "name": "snippet",
      "variables": {
        "variable": "${variable}",
        "string": "${string}",
        "boolean": "${boolean}",
        "object": "${object}",
        "array": "${array}",
        "number": "${number}",
        "integer": "${integer}",
        "default": "${default}",
        "words": "some words"
      }
    }
  ],
  "requires": [
    "dependency"
  ],
  "purpose": [
    "Some purpose."
  ]
}
