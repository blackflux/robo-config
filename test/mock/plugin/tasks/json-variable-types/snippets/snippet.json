{
  "grandparent": {
    "parent": {
      "children": [{
        "prefix${partialKey}": "value${partialValue}",
        "${key}": "${value|UPPER}",
        "object": "${object}"
      }]
    }
  }
}
