{
  "validFor": [
    "json",
    "yml"
  ],
  "description": "Similar to `merge-shallow`, but keeps existing values instead of overwriting.",
  "details": [
    "Useful to ensure specific keys of the target are present without overwriting existing content."
  ]
}
