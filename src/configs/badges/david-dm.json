{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "david-dm",
      "variables": {
        "repo-key": "${repo-key}"
      }
    }
  ],
  "variables": {
    "repo-key": "${repoKey}"
  }
}
