{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "npm-downloads",
      "variables": {
        "repo-name": "${repo-name}"
      }
    }
  ],
  "variables": {
    "repo-name": "${repoName}"
  }
}
