{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "coveralls",
      "variables": {
        "repoKey": "${repoKey}"
      }
    }
  ],
  "requires": [
    "coveralls"
  ],
  "description": "Insert [coveralls](https://coveralls.io/) badge into project README.md [markdown[(https://en.wikipedia.org/wiki/Markdown) file."
}
