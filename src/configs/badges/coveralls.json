{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "coveralls",
      "variables": {
        "repoKey": "${repoKey}"
      }
    }
  ],
  "variables": {
    "repoKey": "${repoKey}"
  }
}
