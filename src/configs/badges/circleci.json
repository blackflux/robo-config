{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "circleci",
      "variables": {
        "repoKey": "${repoKey}"
      }
    }
  ],
  "variables": {
    "repoKey": "${repoKey}"
  }
}
