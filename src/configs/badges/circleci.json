{
  "target": "README.md",
  "strategy": "merge-below-title",
  "snippets": [
    {
      "name": "circleci",
      "variables": {
        "repo-key": "${repo-key}"
      }
    }
  ],
  "variables": {
    "repo-key": "${repoKey}"
  }
}
